---
title: "Page Not Found (legacy)"
sitemap: false
permalink: /404-old.html
layout: none
---

<!doctype html>
<html lang="en">
<head>
  <meta charset="utf-8">
  <title>Redirecting…</title>
  <script>
  (function(){
    var target = 'echostatements.net';
    var h = location.hostname;
    var dotTarget = '.' + target;
    var isTarget = (h === target) || (h.slice(-dotTarget.length) === dotTarget);
    var isLocal = (h === 'localhost') || (h === '127.0.0.1') || (h === '0.0.0.0');
    if (isTarget || isLocal) return;

    var match = (location.search.match(/(?:^|[?&])path=([^&]+)/) || [null, null])[1];
    var path = match ? decodeURIComponent(match) : location.pathname;
    if (!path || path.charAt(0) !== '/') path = '/' + (path || '');
    var dest = 'https://' + target + path + location.hash;
    location.replace(dest);
  })();
  </script>
  <noscript><meta http-equiv="refresh" content="0; url=https://echostatements.net"></noscript>
</head>
<body>
  Redirecting… If you are not redirected, <a href="https://echostatements.net">click here</a>.
</body>
</html>
